import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';
const SearchScreen = () => {
  return (
    <SafeAreaView style={{color: '#fff', flex: 1}}>
      <Text style={{color: '#000', fontSize: 16}}>SearchScreen</Text>
    </SafeAreaView>
  );
};

export default SearchScreen;
