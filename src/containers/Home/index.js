import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';
const HomeScreen = props => {
  return (
    <SafeAreaView style={{flex: 1}}>
      <Text style={{color: '#000', fontSize: 16}}>HomeScreen</Text>
    </SafeAreaView>
  );
};

export default HomeScreen;
